name: PR Code Format Check
on:
  workflow_dispatch:
jobs:
  check-code-format:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch source
        uses: actions/checkout@v4

      - name: Check for installed rust tools
        run: |
          rustup --version
          rustup toolchain list
          rustfmt --help

      - run: echo "status = ${{ job.status }}"
